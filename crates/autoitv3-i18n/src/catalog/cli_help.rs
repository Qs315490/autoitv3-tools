//! Command line help: what `au3 --help` and every `au3 <subcommand> --help`
//! print, plus the messages from argument parsing (`cli.rs`, `args.rs`).
//!
//! The doc comments in those files are the keys; clap renders them into
//! `about`/`long_about`/`help`/`long_help`, and `au3-cli/src/i18n_cli.rs`
//! translates whatever clap stored.

pub static ENTRIES: &[(&str, &str)] = &[
    // clap help surface (`cli.rs` and the shared `Args` types in `args.rs`).
    ("AutoIt v3 analysis toolkit: parse, format, deobfuscate and run scripts", "AutoIt v3 分析工具包：解析、格式化、反混淆与运行脚本"),
    ("Language for messages, help and diagnostics", "消息、帮助和诊断所用的语言"),
    ("Report the `#AutoIt3Wrapper_*` settings the script carries", "报告脚本里带的 `#AutoIt3Wrapper_*` 设置"),
    ("Report the `#AutoIt3Wrapper_*` settings the script carries\n\nThe wrapper consumed them at build time — they are where a build's resources, version info, x64 stub and UPX packing came from — so nothing acts on them, but they are the build's fingerprint: which packer knobs produced the thing being analysed. Off by default.", "报告脚本里带的 `#AutoIt3Wrapper_*` 设置\n\n这些设置是打包期被 wrapper 消费掉的——产物的资源、版本信息、x64 stub、UPX 加壳都出自它们——所以本工具不据此做任何事，但它们是这次构建的**指纹**：手里的东西是用哪套打包开关造出来的。默认关闭。"),
    ("Language for messages, help and diagnostics\n\n`auto` follows the environment and then the host: `AU3_LANG`, then `LC_ALL` / `LC_MESSAGES` / `LANG` (`zh*` is Chinese, anything else English), and on Windows the user's UI language when none of those is set. `en` and `zh-CN` force one instead. Defaults to `auto`.", "消息、帮助和诊断所用的语言\n\n`auto` 依次看环境与本机：`AU3_LANG`，然后是 `LC_ALL` / `LC_MESSAGES` / `LANG`（`zh*` 为中文，其他一律英文），两者都没设置时在 Windows 上看用户的界面语言。`en` 与 `zh-CN` 则直接指定。默认为 `auto`。"),
    ("(internal) Attach to the console of the process that started us", "（内部）附加到启动我们的进程的控制台"),
    ("(internal) Attach to the console of the process that started us\n\nThe elevated copy an `#RequireAdmin` run starts is given a console of its own by the shell, so the script's output would appear in a second window. The launcher passes its own process id so the copy can attach to the console the command was typed in. Not meant to be used by hand: see `autoitv3-platform`'s `elevate` module.", "（内部）附加到启动我们的进程的控制台\n\n`#RequireAdmin` 运行所启动的提权副本会被 shell 分配一个独立控制台，脚本输出就会出现在另一个窗口中。启动器传入自己的进程 ID，使该副本能够附加到输入命令的那个控制台。不供手工使用：参见 `autoitv3-platform` 的 `elevate` 模块。"),
    ("Parse a script and report top-level item / function counts", "解析脚本并报告顶层条目/函数数量"),
    ("Input AutoIt v3 script, or a compiled build (.exe/.a3x) to read it from", "输入的 AutoIt v3 脚本，或要从中读取脚本的编译构建（.exe/.a3x）"),
    ("Re-print normalised source, preserving comments", "重新输出规范化源码，保留注释"),
    ("Write the result to FILE instead of stdout (use `-` for stdout)", "将结果写入 FILE 而非 stdout（使用 `-` 表示 stdout）"),
    ("Constant-fold, resolve the function table, rename identifiers", "常量折叠、解析函数表、重命名标识符"),
    ("Run the script body first and inline the table values it computed, then apply the syntactic passes", "先运行脚本主体并内联其计算出的表值，然后应用语法处理"),
    (
        "Run with AutoIt's own semantics: real delays, real entropy and real side effects",
        "按 AutoIt 自己的语义运行：真实延时、真实熵、真实副作用",
    ),
    (
        "Run with AutoIt's own semantics: real delays, real entropy and real side effects\n\nThe default for `run` and `debug` — running a script should do what the script says. The analysis commands (`evaluate`, `deobfuscate --evaluate`) default to `--deterministic` instead.",
        "按 AutoIt 自己的语义运行：真实延时、真实熵、真实副作用\n\n`run` 与 `debug` 的默认值——跑脚本就该做脚本说的事。分析类命令（`evaluate`、`deobfuscate --evaluate`）默认走 `--deterministic`。",
    ),
    (
        "Run with the deterministic analysis profile: `Sleep` skipped, `Random` from a fixed seed, every side effect refused",
        "按确定性分析配置运行：跳过 `Sleep`、`Random` 用固定种子、拒绝一切副作用",
    ),
    (
        "Run with the deterministic analysis profile: `Sleep` skipped, `Random` from a fixed seed, every side effect refused\n\nA refused side effect returns its failure value with `@error = 1` and is reported once per kind on stderr. The default for `evaluate` and `deobfuscate --evaluate`, which analyse a sample without letting it touch the machine; `run` and `debug` default to `--faithful`.",
        "按确定性分析配置运行：跳过 `Sleep`、`Random` 用固定种子、拒绝一切副作用\n\n被拒绝的副作用会返回失败值并置 `@error = 1`，同时在 stderr 上按类别各报一次。这是 `evaluate` 与 `deobfuscate --evaluate` 的默认值——它们分析样本而不让它碰这台机器；`run` 与 `debug` 默认是 `--faithful`。",
    ),
    ("Allow one class of side effect in the deterministic profile. Repeatable. Kinds: file, env, registry, clipboard, spawn, shutdown, net, process", "在确定性配置中允许某一类副作用。可重复。类别：file、env、registry、clipboard、spawn、shutdown、net、process"),
    ("Deny one class of side effect, even in the faithful profile. Repeatable", "拒绝某一类副作用，即使在保真配置中也是如此。可重复"),
    ("Rename identifiers: give every script-defined variable and function a deterministic `$l_str_003` / `f042` alias. Off by default, so the output keeps the names the script was written with", "重命名标识符：为脚本定义的每个变量和函数赋予确定性的 `$l_str_003` / `f042` 别名。默认关闭，因此输出保留脚本编写时使用的名称"),
    ("Deprecated: renaming is off by default now, so this does nothing", "已废弃：重命名现在默认关闭，因此此选项不起作用"),
    ("Name of the function-table variable (with or without `$`). Detected from the program's shape when omitted", "函数表变量的名称（可带或不带 `$`）。省略时根据程序的形态检测"),
    ("Name of the function that builds the function table. Detected from the program's shape when omitted", "构建函数表的函数名称。省略时根据程序的形态检测"),
    ("Rewrite `Global Const $t = Build()` into the table's literal value", "将 `Global Const $t = Build()` 改写为表的字面量值"),
    ("Maximum interpreter steps before giving up (0 = no limit)", "放弃前的解释器最大步数（0 = 无限制）"),
    ("Do not print the periodic progress heartbeat for long evaluations", "不为长时间求值打印周期性进度心跳"),
    ("Answer `@Compiled = 1` even when the input is `.au3` source", "即使输入是 `.au3` 源码也返回 `@Compiled = 1`"),
    ("Answer `@Compiled = 0` even when the input is a compiled build", "即使输入是编译构建也返回 `@Compiled = 0`"),
    ("Add DIR to the `#include` search path (repeatable)", "将 DIR 添加到 `#include` 搜索路径（可重复）"),
    ("Do not expand `#include` at all: run the script as if the directives were not there", "完全不展开 `#include`：像指令不存在一样运行脚本"),
    ("Emulated Windows version: xp, vista, 7, 8, 81, 10, 11", "模拟的 Windows 版本：xp、vista、7、8、81、10、11"),
    ("Emulated architecture: x86, x64, arm64", "模拟的架构：x86、x64、arm64"),
    ("PE image whose resources `FindResourceW`/`LoadResource` answer from (usually found automatically next to the script)", "`FindResourceW`/`LoadResource` 从中取资源的 PE 映像（通常会自动在脚本旁边找到）"),
    ("Do not install the emulation layer; Windows-only calls become undefined-function errors", "不安装模拟层；仅限 Windows 的调用将变成未定义函数错误"),
    ("Host directory the emulated `C:` drive stands for (default: the host root, so `C:\\home\\me\\a.dat` is `/home/me/a.dat`; empty disables the mapping)", "模拟的 `C:` 盘所代表的主机目录（默认：主机根目录，因此 `C:\\home\\me\\a.dat` 即 `/home/me/a.dat`；留空则禁用映射）"),
    ("Leave `C:` unmapped: every path the script builds is taken as a host path, exactly as the emulation did before the drive map existed", "不映射 `C:`：脚本构建的每个路径都视为主机路径，与驱动器映射出现之前的模拟行为完全一致"),
    ("Route a function area through the emulation layer even where a native implementation exists. Repeatable. Areas: `registry` (Reg*), `clipboard` (Clip*), or a raw function name (`RegWrite`)", "即使存在本机实现，也将某个函数区域交由模拟层处理。可重复。区域：`registry`（Reg*）、`clipboard`（Clip*），或原始函数名（`RegWrite`）"),
    ("Run the script body and inline the table values it computed", "运行脚本主体并内联其计算出的表值"),
    ("Run a script, or call one function from it (probes the obfuscator's builders)", "运行脚本，或从中调用一个函数（探查混淆器的构建器）"),
    ("Function to call; omitted runs the whole script body", "要调用的函数；省略则运行整个脚本主体"),
    ("Script command-line argument (`$CmdLine`); repeat for more. Available in every mode, so a function call can still hand the script body a command line (`--init`)", "脚本命令行参数（`$CmdLine`）；可重复。在所有模式下都可用，因此函数调用仍可向脚本主体传入命令行（`--init`）"),
    ("Argument for the function; repeat for more. Without a FUNC it is a command-line argument instead (same as `--cmdline`). Integers (decimal, or `0x` hex) become numbers, anything else a string", "传给函数的参数；可重复。没有 FUNC 时它改为命令行参数（与 `--cmdline` 相同）。整数（十进制或 `0x` 十六进制）变为数字，其他内容为字符串"),
    ("When a FUNC is named, run the top-level script first so global tables (`$fn_table`, ...) exist before the call", "指定 FUNC 时，先运行顶层脚本，使全局表（`$fn_table` 等）在调用前已存在"),
    ("Print the executed statement stream to stderr", "将已执行的语句流打印到 stderr"),
    ("GUI backend: `auto` (the default) uses the platform's own — real Win32 controls on Windows, nothing drawn elsewhere — except under the deterministic profile, where it means `headless` (an analysis must not open windows or dialogs that wait for somebody); `headless` never draws; `native` is the Windows-native backend (real Win32 windows and controls, needs a Windows host); `egui` opens an eframe window (needs a build with the `gui-egui` feature)", "GUI 后端：`auto`（默认）使用平台自身的实现——Windows 上为真实 Win32 控件，其他平台不绘制任何内容——但在确定性配置下 `auto` 就是 `headless`（分析不该开窗口、更不该弹一个等人点的对话框）；`headless` 从不绘制；`native` 是 Windows 原生后端（真实 Win32 窗口与控件，需要 Windows 主机）；`egui` 打开 eframe 窗口（需要启用 `gui-egui` 特性的构建）"),
    ("Ignore `#RequireAdmin`: run the script in this, unelevated, process", "忽略 `#RequireAdmin`：在此非提权进程中运行脚本"),
    ("Ignore `#RequireAdmin`: run the script in this, unelevated, process\n\nThe default is to honour it the way the interpreter does — start an elevated copy through the shell's `runas` verb and let that copy run the script. Pass this to analyse a script that asks for rights it does not get, or to avoid the UAC prompt in an unattended run.", "忽略 `#RequireAdmin`：在此非提权进程中运行脚本\n\n默认行为与解释器一致——通过 shell 的 `runas` 动词启动提权副本，并让该副本运行脚本。若要分析请求不到权限的脚本，或在无人值守运行时避免 UAC 提示，请传入此选项。"),
    ("(internal) Run as the elevated copy an `#RequireAdmin` run started", "（内部）作为 `#RequireAdmin` 运行所启动的提权副本运行"),
    ("(internal) Run as the elevated copy an `#RequireAdmin` run started\n\nOur own launcher passes this together with `--attach-console`; it says \"the elevation already happened\", so the directive is not acted on again and not reported as skipped either. Not meant to be used by hand.", "（内部）作为 `#RequireAdmin` 运行所启动的提权副本运行\n\n我们自己的启动器会连同 `--attach-console` 一起传入；它表示“提权已经完成”，因此该指令不会被再次执行，也不会被报告为已跳过。不供手工使用。"),
    ("Load a script and debug it: breakpoints, stepping, a prompt", "加载脚本并调试：断点、单步执行、交互提示符"),
    ("Command to run at startup; repeat for a whole session", "启动时运行的命令；可重复以组成完整会话"),
    ("Command to run at startup; repeat for a whole session.\n\nCommands are accepted wherever a prompt would appear, including at a breakpoint, so `-c run -c next -c quit` walks the script. Several commands may share one `-c` if they are separated by `;` (`-c \"break 11; run; print $i\"`); a `;` inside a \"quoted string\" is left alone.", "启动时运行的命令；可重复以组成完整会话。\n\n凡是出现提示符的地方都接受命令，包括在断点处，因此 `-c run -c next -c quit` 可以逐步走完脚本。多条命令若用 `;` 分隔可共用一个 `-c`（`-c \"break 11; run; print $i\"`）；\"带引号字符串\" 内的 `;` 不会被视为分隔符。"),
    ("File of commands to run at startup, one per line; repeat for more", "启动时要运行的命令文件，每行一条；可重复以添加更多"),
    ("File of commands to run at startup, one per line; repeat for more.\n\nBlank lines and lines starting with `#` are ignored, and `;` separates several commands on one line. These run before any `-c` command, and the session stays interactive afterwards — `-x setup.au3dbg` is how you get \"load my usual breakpoints, then hand me the prompt\".", "启动时要运行的命令文件，每行一条；可重复以添加更多。\n\n空行和以 `#` 开头的行会被忽略，`;` 用于分隔同一行上的多条命令。这些命令在任何 `-c` 命令之前运行，之后会话仍保持交互——`-x setup.au3dbg` 可用于“加载我常用的断点，然后把提示符交给我”。"),
    ("Do not stop when a statement fails with an uncaught error", "语句因未捕获错误而失败时不停止"),
    ("Do not stop when a statement fails with an uncaught error\n\nThe default is to stop where the error was raised — the frame that raised it is still on the stack, so it can be inspected like a breakpoint. Use `catch off` at the prompt to change it mid-session.", "语句因未捕获错误而失败时不停止\n\n默认在错误抛出处停止——抛出该错误的栈帧仍在栈上，因此可以像断点一样检查。可在提示符处使用 `catch off` 在会话中途更改。"),
    ("Stop on the first statement the script executes, as if `step` had been typed before `run`", "在脚本执行的第一条语句处停止，如同在 `run` 之前输入了 `step`"),
    ("GUI backend: `auto` (the default) is the platform's own — real Win32 controls on Windows, nothing drawn elsewhere — except under the deterministic profile, where it means `headless` (an analysis must not open windows or dialogs that wait for somebody); `headless` answers the GUI functions without drawing anything on any host; `native` is the Windows-native backend (real Win32 windows and controls, needs a Windows host); `egui` runs the session under an eframe window (needs a build with the `gui-egui` feature)", "GUI 后端：`auto`（默认）使用平台自身的实现——Windows 上为真实 Win32 控件，其他平台不绘制任何内容——但在确定性配置下 `auto` 就是 `headless`（分析不该开窗口、更不该弹一个等人点的对话框）；`headless` 在任何主机上都应答 GUI 函数而不绘制任何内容；`native` 是 Windows 原生后端（真实 Win32 窗口与控件，需要 Windows 主机）；`egui` 在 eframe 窗口下运行会话（需要启用 `gui-egui` 特性的构建）"),
    ("Recover a build's payload: its compiled script (`--script`) or a resource-packed payload", "恢复构建的负载：其编译脚本（`--script`）或资源打包的负载"),
    ("A directory of resource files, a PE image, or a compiled-script chunk", "资源文件目录、PE 映像或编译脚本块"),
    ("Read the compiled script (`AU3!EA05`/`AU3!EA06`) instead of a resource-packed payload, and emit its `.au3` source", "读取编译脚本（`AU3!EA05`/`AU3!EA06`）而非资源打包负载，并输出其 `.au3` 源码"),
    ("Emit the concatenated payload instead of one entry per line", "输出拼接后的负载，而非每行一个条目"),
    ("Number the entries, so an index can be read straight off a disassembly or a debugger session", "为条目编号，便于直接从反汇编或调试器会话中读取索引"),
    ("Only these entries: a 1-based index, or a comma-separated list with inclusive ranges (`152`, `1-5,3148`)", "仅这些条目：从 1 开始的索引，或用逗号分隔且范围闭合的列表（`152`、`1-5,3148`）"),
    // Runtime messages from `args.rs`.
    ("unknown effect {shown} (try file, env, registry, clipboard, spawn, shutdown, net, process)", "未知的副作用 {shown}（请尝试 file、env、registry、clipboard、spawn、shutdown、net、process）"),
    ("effect {shown} given to both --allow and --deny", "副作用 {shown} 同时传给了 --allow 和 --deny"),
    ("unknown --win-version {shown} (try win7, win8, win81, win10, win11)", "未知的 --win-version {shown}（请尝试 win7、win8、win81、win10、win11）"),
    ("unknown --win-arch {shown} (try x86, x64, arm64)", "未知的 --win-arch {shown}（请尝试 x86、x64、arm64）"),
    ("--resource-module {path}: no such file", "--resource-module {path}：没有这个文件"),
    ("# resource module: {path} (the input build; override with --resource-module)", "# 资源模块：{path}（输入构建；可用 --resource-module 覆盖）"),
    ("# resource module: {path} (found next to the script; override with --resource-module)", "# 资源模块：{path}（在脚本旁边找到；可用 --resource-module 覆盖）"),
    ("# no resource image, __* files or _Res_File_Add entry found: resource calls may fail", "# 未找到资源映像、__* 资源文件或 _Res_File_Add 条目：资源调用可能失败"),
    ("cannot read {path}: {e}", "无法读取 {path}：{e}"),
    ("cannot read {path}: not UTF-8 or UTF-16 source, and not a compiled build (no MZ / AU3!EA header)", "无法读取 {path}：既不是 UTF-8 或 UTF-16 源码，也不是编译构建（没有 MZ / AU3!EA 头）"),
    ("parse error in {path}: {e}", "解析 {path} 时出错：{e}"),
    ("# #include: read {files} files ({included} included)", "# #include：读取了 {files} 个文件（包含 {included} 个）"),
    ("# input build: {path} (compiled script {version}, {files} embedded file(s))", "# 输入构建：{path}（编译脚本 {version}，嵌入 {files} 个文件）"),
    ("# AutoIt3Wrapper settings ({count}): {list}", "# AutoIt3Wrapper 设置（{count} 项）：{list}"),
    ("parse error in the script unpacked from {path}: {e}", "解析从 {path} 解包出的脚本时出错：{e}"),
    // clap writes these itself (help headings, usage errors); `i18n_cli` replaces them after rendering.
    ("Usage:", "用法:"),
    ("[possible values: ", "[可选值："),
    ("[default: ", "[默认值："),
    ("Commands:", "子命令:"),
    ("Options:", "选项:"),
    ("Arguments:", "参数:"),
    ("For more information, try '--help'.", "更多信息请运行 '--help'。"),
    ("error: ", "错误: "),
    ("tip: ", "提示: "),
    ("unexpected argument {rest}", "无法识别的参数 {rest}"),
    ("unexpected argument {value} found", "无法识别的参数 {value}"),
    (
        "to pass {value} as a value, use {hint}",
        "要把 {value} 当作值传入，请用 {hint}",
    ),
    ("unrecognized subcommand {rest}", "无法识别的子命令 {rest}"),
    ("invalid value {rest}", "无效的取值 {rest}"),
    (
        "invalid value {value} for {arg}: {detail}",
        "无效的取值 {value}（{arg}）：{detail}",
    ),
    ("a value is required for {rest}", "{rest} 缺少取值"),
    ("the following required arguments were not provided:", "以下必需参数没有提供："),
    (
        "Print this message or the help of the given subcommand(s)",
        "打印本消息或给定子命令的帮助",
    ),
    ("Print help", "打印帮助"),
    (
        "Ignore `#RequireAdmin`: debug the script in this, unelevated, process",
        "忽略 `#RequireAdmin`：就在当前（非提权）进程里调试",
    ),
    (
        "Ignore `#RequireAdmin`: debug the script in this, unelevated, process\n\nThe default is to honour it the way the interpreter does — start an elevated copy through the shell's `runas` verb, and let that copy run the session. `--attach-console` hands the copy this window's console, so the prompt and the output stay where the command was typed. Pass this to debug a script that asks for rights it does not get, or to avoid the UAC prompt; a session whose input or output is not a terminal stays here anyway (there is no console to hand over).",
        "忽略 `#RequireAdmin`：就在当前（非提权）进程里调试\n\n默认按解释器的做法处理——用 shell 的 `runas` 动词起一个提权副本，由那个副本跑这个会话；`--attach-console` 把本窗口的控制台交给副本，所以提示符和输出都留在你敲命令的地方。要调试一个申请了权限却拿不到权限的脚本、或想避开 UAC 提示时用它；输入或输出不是终端的会话本来也会留在这里（没有控制台可交）。",
    ),
    (
        "(internal) Run as the elevated copy an `#RequireAdmin` session started",
        "（内部）作为 `#RequireAdmin` 会话启动的提权副本运行",
    ),
    (
        "(internal) Run as the elevated copy an `#RequireAdmin` session started\n\nOur own launcher passes this together with `--attach-console`; it says \"the elevation already happened\", so the directive is not acted on again and not reported as skipped either. Not meant to be used by hand.",
        "（内部）作为 `#RequireAdmin` 会话启动的提权副本运行\n\n我们自己的启动器把它和 `--attach-console` 一起传进来；它表示“提权已经发生”，于是不再对该指令采取行动，也不报告为已跳过。不要手工使用。",
    ),
    ("Print help (see more with '--help')", "打印帮助（用 '--help' 查看更多）"),
    ("Print help (see a summary with '-h')", "打印帮助（用 '-h' 看摘要）"),
    ("Print version", "打印版本"),
    (
        "--gui native needs a Windows host: the native backend is the real Win32 one",
        "--gui native 需要 Windows 主机：原生后端就是真实的 Win32 实现",
    ),
];
