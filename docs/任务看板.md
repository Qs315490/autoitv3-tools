# AutoIt v3 内置函数实现任务看板

> **用途**：把 AutoIt v3 官方函数表中尚未落地的内置函数整理成可执行的任务清单，
> 并按「通用」与「需要仿真」分开，供 `autoitv3-runtime` / `autoitv3-platform` 逐项实现。
>
> **依据**：AutoIt v3 官方函数表（`functions.htm`，共 **403** 个内置函数）
> × 本仓库现状（`crates/autoitv3-runtime/src/builtins.rs`、
> `crates/autoitv3-platform/src/{common,linux,windows}/`、`.../winemu/`）
> × 真实脚本对内置函数的使用情况。
>
> **配套文件**：逐函数机器可读状态表见 `docs/函数实现状态.tsv`
> （由源码的 match 分支整理生成）。

---

## 进度快照

| 阶段 | 状态 |
| ---- | ---- |
| **列 A · 通用（67）** | ✅ **已全部实现并有回归测试** —— 核心语言 16、文件/INI 16、进程/标准 IO 10、网络 25 |
| 列 B · 需要仿真 · 可落地（28） | 🚧 待办（其中若干项真实脚本已调用） |
| 列 C · 需要仿真 · GUI/窗口/输入（165） | 🚧 待办（其中数十项真实脚本已调用） |

「通用」部分的落地位置：

- `builtins.rs`：`Assign`/`Eval`/`IsDeclared`/`FuncName`、`IsBool`/`IsFloat`、`ChrW`、
  `BitRotate`、`MapAppend`、`StringIsASCII`/`Lower`/`Upper`/`XDigit`、`StringStripCR`、
  `StringFromASCIIArray`/`StringToASCIIArray`（`interp.rs` 新增按名读写变量接口）
- `common/`：`FileChangeDir`/`GetPos`/`SetPos`/`SetEnd`/`GetEncoding`/`ReadToArray`/
  `FindFirstFile`/`FindNextFile`/`SetTime`，以及全部 7 个 `Ini*`
- 通用层内新增进程/网络子服务（`common/proc.rs` + `common/net.rs`）：`Run`/`RunWait`/`ProcessWait`/
  `ProcessWaitClose`/`ProcessGetStats`/`ProcessSetPriority`/`StdoutRead`/`StderrRead`/
  `StdinWrite`/`StdioClose`，`Inet*`/`TCP*`/`UDP*`/`Ping`/代理设置。
  该层安装在所有平台，受 `ExecutionProfile` 门控：确定性分析配置一律拒绝启动进程或联网。

---

## 图例

| 维度 | 取值 | 含义 |
| ---- | ---- | ---- |
| 状态 | ✅ 已实现 | 能在解释器里解析，且有真实语义 |
| | 🚧 待实现 | 官方函数表里有、本仓库没有 |
| 归属层 | 核心 | `builtins.rs`——平台无关的语言级函数 |
| | 通用 | `common/`（`mod.rs` + `proc.rs` + `net.rs`）——文件/环境/数学 + 进程/网络 |
| | 系统 | `linux/` / `windows/`——宿主 OS 等价实现 |
| | 仿真 | `winemu/`——必须提供 Windows 语义 |
| 优先级 | **P0** | 真实脚本已在调用，落地后能继续往下跑 |
| | **P1** | 真实脚本未调用，但基础能力 / 仿真层已有地基可快速接上 |
| | **P2** | 低频或完善性，GUI/托盘等重子系统内部的其他项 |

> **分类口径**：`通用` = 不依赖 Windows 身份、注册表、COM、窗口管理器即可实现
> （含用宿主 socket / 进程实现的网络与执行）；`需要仿真` = 必须由 `winemu` 回答
> Windows 专有语义。

---

## 一、总览

| 指标 | 数量 |
| ---- | ----: |
| 官方内置函数总数 | 403 |
| ✅ 已实现 | **210** |
| 🚧 待实现 | 193 |
| └ 待实现 · 通用 | **0** |
| └ 待实现 · 需要仿真（可落地） | 28 |
| └ 待实现 · 需要仿真（GUI/窗口/输入 重子系统） | 165 |
| 真实脚本已调用但尚未实现（P0） | 数十项 |
| └ P0 · 通用 | 0 |
| └ P0 · 需要仿真（可落地） | 少量（见 2.1） |
| └ P0 · 需要仿真（GUI/窗口/输入） | 数十项（见 2.2） |

**当前结论**：通用部分已清零。剩下的 193 项全是 Windows 语义，
其中 28 项可借助 `winemu` 现有地基（`pe.rs`/`version.rs`/注册表/驱动器）落地，
真正的大头仍是 GUI/窗口/输入子系统（165 项），而真实脚本对它的调用非常密集
（`GUICtrlCreateGraphic`、`GUISwitch`、`GUICtrlSetBkColor`、`GUICtrlSendMsg` 都居前）。

---

## 二、🔥 优先队列：真实脚本已调用但尚未实现

### 2.1 需要仿真 · 可落地

| 函数 | 说明 | 落地要点 |
| ---- | ---- | ---- |
| `FileGetVersion` | 读 PE 文件版本信息 | `winemu/version.rs` 已有版本数据结构，接 `pe.rs` 即可 |
| `IsAdmin` | 是否管理员 | 固定返回 `winemu` 选定机器的权限状态即可 |
| `MemGetStats` | 内存统计 | 返回一台仿真机器的固定内存表 |
| `FileRecycle` | 删除到回收站 | `winemu` 维护一个回收站目录，写入按 `ExecutionProfile` 门控 |
| `DllCallbackRegister` | 注册回调 | 分配假回调指针 + 名称表即可 |
| `DllCallbackGetPtr` | 取回调指针 | 同上 |
| `ObjGet` | 取已存在 COM 对象 | 无 COM 运行时的前提下返回可判定的失败（`@error`） |
| `IsObj` | 是否对象类型 | 与 `Value` 增加 Obj 变体一起做 |
| `ShellExecute` | 系统外壳启动 | `winemu` 记录启动意图，按配置决定是否映射到宿主 |
| `Shutdown` | 关机/重启 | 仿真层记录请求并返回成功，不真正关机 |

### 2.2 需要仿真 · GUI/窗口/输入

真实脚本对 GUI 的调用非常密集，按相对热度分组：

- **高频**：`GUICtrlCreateGraphic`、`GUISwitch`、`GUICtrlSetBkColor`、`GUICtrlSendMsg`、`MsgBox`
- **中频**：`GUICtrlSetState`、`WinGetState`、`GUIDelete`、`GUICtrlCreateLabel`、`GUICtrlSetColor`、
  `FileOpenDialog`、`FileSaveDialog`、`FileSelectFolder`
- **其余（最小可用集）**：`GUICtrlSetResizing`、`GUICtrlSetData`、`GUICtrlSetCursor`、`GUISetState`、
  `GUIGetMsg`、`GUIGetCursorInfo`、`GUICtrlDelete`、`WinGetPos`、`GUIRegisterMsg`、`GUICtrlSetTip`、
  `GUICtrlCreatePic`、`GUICtrlCreateButton`、`GUICtrlSetPos`、`GUICtrlRead`、`GUICtrlGetHandle`、
  `GUICreate`、`SplashTextOn`、`SplashOff`、`GUICtrlSetImage`、`GUICtrlGetState`、`GUICtrlCreateMenuItem`、
  `WinMove`、`WinGetClientSize`、`GUISetFont`、`GUISetBkColor`、`GUICtrlSetFont`、`GUICtrlCreateListView`、
  `GUICtrlCreateContextMenu`、`GUICtrlCreateInput`、`ControlGetPos`、`ControlClick`、`MouseGetPos`、`Send`

> 上表即「无头 GUI 仿真」里程碑的最小可用集：控件可以只做**内存对象 + 假句柄**，
> `GUIGetMsg` 在无人操作时返回 `0`，让脚本的消息循环不空转、能继续走业务流程。

---

## 三、列 A：✅ 已完成 · 通用（67）

全部已落地并有回归测试；逐项状态见 `docs/函数实现状态.tsv`。

| 分组 | 落地位置 | 函数 |
| ---- | ---- | ---- |
| 核心语言（16） | `builtins.rs` / `interp.rs` | `Assign` `Eval` `IsDeclared` `FuncName` `IsBool` `IsFloat` `ChrW` `BitRotate` `MapAppend` `StringIsASCII` `StringIsLower` `StringIsUpper` `StringIsXDigit` `StringStripCR` `StringFromASCIIArray` `StringToASCIIArray` |
| 文件 / 目录（9） | `common/` | `FileChangeDir` `FileGetPos` `FileSetPos` `FileSetEnd` `FileGetEncoding` `FileReadToArray` `FileFindFirstFile` `FileFindNextFile` `FileSetTime` |
| INI（7） | `common/` | `IniRead` `IniWrite` `IniDelete` `IniReadSection` `IniReadSectionNames` `IniRenameSection` `IniWriteSection` |
| 进程 / 执行 / 标准 IO（10） | `common/proc.rs` | `Run` `RunWait` `ProcessWait` `ProcessWaitClose` `ProcessGetStats` `ProcessSetPriority` `StdoutRead` `StderrRead` `StdinWrite` `StdioClose` |
| 网络（25） | `common/net.rs` | `InetGet` `InetGetInfo` `InetGetSize` `InetRead` `InetClose` `FtpSetProxy` `HttpSetProxy` `HttpSetUserAgent` `Ping` `TCPStartup` `TCPShutdown` `TCPConnect` `TCPListen` `TCPAccept` `TCPRecv` `TCPSend` `TCPCloseSocket` `TCPNameToIP` `UDPStartup` `UDPShutdown` `UDPOpen` `UDPBind` `UDPRecv` `UDPSend` `UDPCloseSocket` |

**已记录的近似**（与 README「不静默编造值」一致）：

- `FileGetEncoding` 返回 AutoIt 的 `32/64/128/256/512`，纯 ASCII 返回 `0`
- `FileFindNextFile` 的 `flag = 1`（属性字符串写入 `@extended`）无法表达，退化为 `0`
- `FileSetTime` 的创建时间（`type = 1`）在宿主上不可设置，返回失败
- `StringIsLower`/`Upper` 要求至少含一个字母，且所有字母大小写一致
- `MapAppend` 取"下一个未占用的正整数键"；Map 键在本运行时是字符串，故整数键与
  同数字的字符串键无法区分
- `Run` 的 `show_flag` 在非 Windows 上忽略；`$STDERR_MERGED` 用缓冲区拼接模拟
- `ProcessSetPriority` 只报告成功，不真正改 nice；`ProcessWait`/`WaitClose` 阻塞等待，
  确定性分析配置下立即返回
- `Inet*` 只支持明文 `http://`（无 TLS），`https://` 立即失败；`TCPAccept` 非阻塞
- 网络/进程副作用全部受 `ExecutionProfile` 门控，`deterministic()` 下拒绝执行

---

## 四、列 B：待办 · 需要仿真 · 可落地（28）

这几类是 Windows 专有语义，但 `winemu` 已有可用地基（`pe.rs`、`version.rs`、
资源解析、`DllStruct`），属于「难在语义、不难在基建」。

### B1 Windows 文件 / PE（7）

| 函数 | 状态 | 优先级 | 落地要点 |
| ---- | ---- | ---- | ---- |
| `FileGetVersion` | 🚧 | **P0** | 解析 PE 的 VS_VERSION_INFO；`version.rs`/`pe.rs` 已有基础 |
| `FileCreateShortcut` | 🚧 | P1 | 生成 `.lnk` 或至少记录快捷方式意图 |
| `FileGetShortcut` | 🚧 | P1 | 解析 `.lnk` |
| `FileCreateNTFSLink` | 🚧 | P2 | NTFS 硬链接；宿主无对应概念时按失败返回 |
| `FileRecycle` | 🚧 | **P0** | 移入仿真回收站目录 |
| `FileRecycleEmpty` | 🚧 | P1 | 清空仿真回收站 |
| `FileInstall` | 🚧 | P1 | 从编译产物内嵌资源落盘（与 `au3 unpack`/资源层衔接） |

### B2 DllCall 扩展 / 回调（4）

| 函数 | 状态 | 优先级 | 落地要点 |
| ---- | ---- | ---- | ---- |
| `DllCallAddress` | 🚧 | P1 | 复用 `DllCall` 的签名解析，地址→已知例程映射 |
| `DllCallbackRegister` | 🚧 | **P0** | 假回调指针表；真实脚本在用 |
| `DllCallbackGetPtr` | 🚧 | **P0** | 返回上一步分配的指针 |
| `DllCallbackFree` | 🚧 | P1 | 释放表项 |

### B3 COM（6）

| 函数 | 状态 | 优先级 | 落地要点 |
| ---- | ---- | ---- | ---- |
| `ObjCreate` | 🚧 | P1 | 需先在 `Value` 增加 Obj 变体；无 COM 运行时则返回可判定失败 |
| `ObjCreateInterface` | 🚧 | P2 | 同上 |
| `ObjEvent` | 🚧 | P2 | 同上 |
| `ObjGet` | 🚧 | **P0** | 真实脚本在用；短路径是「可判定失败」而非编造对象 |
| `ObjName` | 🚧 | P2 | 同上 |
| `IsObj` | 🚧 | **P0** | 与 `Value::Obj` 一起加 |

### B4 系统信息 / 驱动器（6）

| 函数 | 状态 | 优先级 | 落地要点 |
| ---- | ---- | ---- | ---- |
| `MemGetStats` | 🚧 | **P0** | 返回仿真机器的固定内存表 |
| `IsAdmin` | 🚧 | **P0** | 由 `WindowsEmulation` 配置决定 |
| `DriveMapAdd` | 🚧 | P1 | 仿真网络驱动器映射表 |
| `DriveMapDel` | 🚧 | P1 | 删除映射 |
| `DriveMapGet` | 🚧 | P1 | 查映射 |
| `DriveSetLabel` | 🚧 | P2 | 改卷标（写 `DriveSpec`） |

### B5 执行 / Shell（5）

| 函数 | 状态 | 优先级 | 落地要点 |
| ---- | ---- | ---- | ---- |
| `ShellExecute` | 🚧 | **P0** | 真实脚本在用；记录启动意图，是否映射到宿主由配置决定 |
| `ShellExecuteWait` | 🚧 | P1 | 同上并等待 |
| `RunAs` | 🚧 | P2 | 凭据语义，仿真层可忽略凭据直接委托 |
| `RunAsWait` | 🚧 | P2 | 同上 |
| `Shutdown` | 🚧 | **P0** | 记录请求并返回成功，不真正关机 |

---

## 五、列 C：待办 · 需要仿真 · GUI / 窗口 / 输入（165）

> 现状：这一层是 README 明确写下的**边界**——非 Windows 主机上它们如实报
> `undefined function`。但真实脚本对 GUI 的调用极其密集，
> 因此建议单独立项「**无头 GUI 仿真**」：控件 = 内存对象，句柄 = 整数，
> `GUIGetMsg` 无事件时返回 `0`，`GUICtrlRead` 返回脚本自己设进去的数据。
> 目标不是像素级还原，而是让业务流程能继续推进。

### C1 文件选择对话框（3）

`FileOpenDialog` **(P0)** · `FileSaveDialog` **(P0)** · `FileSelectFolder` **(P0)**

### C2 GUI 窗口与控件（71）

- **GUI 主流程（18）**：`GUICreate` **(P0)** · `GUIDelete` **(P0)** · `GUIGetMsg` **(P0)** ·
  `GUIGetCursorInfo` **(P0)** · `GUIRegisterMsg` **(P0)** · `GUISetState` **(P0)** ·
  `GUISwitch` **(P0)** · `GUISetBkColor` **(P0)** · `GUISetFont` **(P0)** ·
  `GUISetAccelerators` · `GUISetCoord` · `GUISetCursor` · `GUISetHelp` · `GUISetIcon` ·
  `GUISetOnEvent` · `GUISetStyle` · `GUIGetStyle` · `GUIStartGroup`
- **`GUICtrlCreate*`（29）**：`...Graphic` **(P0)** · `...Label` **(P0)** · `...Button` **(P0)** ·
  `...Pic` **(P0)** · `...MenuItem` **(P0)** · `...ContextMenu` **(P0)** · `...Input` **(P0)** ·
  `...ListView` **(P0)** · `...Avi` · `...Checkbox` · `...Combo` · `...Date` · `...Dummy` ·
  `...Edit` · `...Group` · `...Icon` · `...List` · `...ListViewItem` · `...Menu` · `...MonthCal` ·
  `...Obj` · `...Progress` · `...Radio` · `...Slider` · `...Tab` · `...TabItem` · `...TreeView` ·
  `...TreeViewItem` · `...Updown`
- **`GUICtrl` 查询/消息（8）**：`GUICtrlSendMsg` **(P0)** · `GUICtrlGetHandle` **(P0)** ·
  `GUICtrlGetState` **(P0)** · `GUICtrlRead` **(P0)** · `GUICtrlDelete` **(P0)** ·
  `GUICtrlRecvMsg` · `GUICtrlSendToDummy` · `GUICtrlRegisterListViewSort`
- **`GUICtrlSet*`（16）**：`...BkColor` **(P0)** · `...State` **(P0)** · `...Color` **(P0)** ·
  `...Cursor` **(P0)** · `...Data` **(P0)** · `...Resizing` **(P0)** · `...Font` **(P0)** ·
  `...Image` **(P0)** · `...Pos` **(P0)** · `...Tip` **(P0)** · `...Graphic` · `...Limit` ·
  `...OnEvent` · `...Style` · `...DefBkColor` · `...DefColor`

### C3 窗口管理（27）

`WinGetState` **(P0)** · `WinGetPos` **(P0)** · `WinMove` **(P0)** · `WinGetClientSize` **(P0)** ·
`StatusbarGetText` · `WinActivate` · `WinActive` · `WinClose` · `WinExists` · `WinGetCaretPos` ·
`WinGetClassList` · `WinGetHandle` · `WinGetProcess` · `WinGetText` · `WinGetTitle` · `WinKill` ·
`WinList` · `WinMenuSelectItem` · `WinMinimizeAll` · `WinMinimizeAllUndo` · `WinSetOnTop` ·
`WinSetState` · `WinSetTitle` · `WinWait` · `WinWaitActive` · `WinWaitClose` · `WinWaitNotActive`

### C4 控件操作（16）

`ControlClick` **(P0)** · `ControlGetPos` **(P0)** · `ControlCommand` · `ControlDisable` ·
`ControlEnable` · `ControlFocus` · `ControlGetFocus` · `ControlGetHandle` · `ControlGetText` ·
`ControlHide` · `ControlListView` · `ControlMove` · `ControlSend` · `ControlSetText` ·
`ControlShow` · `ControlTreeView`

### C5 输入 / 鼠标 / 对话框（14）

`MsgBox` **(P0)** · `MouseGetPos` **(P0)** · `Send` **(P0)** · `InputBox` · `MouseClick` ·
`MouseClickDrag` · `MouseDown` · `MouseGetCursor` · `MouseMove` · `MouseUp` · `MouseWheel` ·
`SendKeepActive` · `HotKeySet` · `BlockInput`

### C6 像素（3）

`PixelChecksum` · `PixelGetColor` · `PixelSearch`

### C7 界面反馈 / 托盘（24）

`SplashTextOn` **(P0)** · `SplashOff` **(P0)** · `SplashImageOn` · `ProgressOn` · `ProgressSet` ·
`ProgressOff` · `ToolTip` · `TrayCreateItem` · `TrayCreateMenu` · `TrayGetMsg` · `TrayItemDelete` ·
`TrayItemGetHandle` · `TrayItemGetState` · `TrayItemGetText` · `TrayItemSetOnEvent` ·
`TrayItemSetState` · `TrayItemSetText` · `TraySetClick` · `TraySetIcon` · `TraySetOnEvent` ·
`TraySetPauseIcon` · `TraySetState` · `TraySetToolTip` · `TrayTip`

### C8 声音 / 光驱 / AutoIt 窗口（6）

`Beep` · `SoundPlay` · `SoundSetWaveVolume` · `CDTray` · `AutoItWinGetTitle` · `AutoItWinSetTitle`

> 另有一个非 GUI 但属于 Windows 语义的 `Break`（托盘菜单退出开关），随托盘子系统一起处理。

---

## 六、看板 · 列 D：已实现基线（210）

分类汇总，逐项清单见 `docs/函数实现状态.tsv`。新增功能时不要重复实现这些。

| 类别 | 代表函数 |
| ---- | ---- |
| 数学 / 位运算 | `Abs` `ACos` `ASin` `ATan` `BitAND` `BitNOT` `BitOR` `BitRotate` `BitShift` `BitXOR` `Ceiling` `Cos` `Dec` `Exp` `Floor` `Hex` `Int` `Log` `Mod` `Number` `Random` `Round` `Sin` `Sqrt` `SRandom` `Tan` |
| 字符串 | `String` `StringAddCR` `StringCompare` `StringFormat` `StringFromASCIIArray` `StringInStr` `StringIsAlNum` `StringIsAlpha` `StringIsASCII` `StringIsDigit` `StringIsFloat` `StringIsInt` `StringIsLower` `StringIsSpace` `StringIsUpper` `StringIsXDigit` `StringLeft` `StringLen` `StringLower` `StringMid` `StringRegExp` `StringRegExpReplace` `StringReplace` `StringReverse` `StringRight` `StringSplit` `StringStripCR` `StringStripWS` `StringToASCIIArray` `StringToBinary` `StringTrimLeft` `StringTrimRight` `StringUpper` `Asc` `AscW` `Chr` `ChrW` |
| 数组 / Map / 类型 | `UBound` `IsArray` `IsBinary` `IsBool` `IsFloat` `IsFunc` `IsInt` `IsKeyword` `IsMap` `IsNumber` `IsPtr` `IsString` `IsHWnd` `IsDeclared` `MapAppend` `MapExists` `MapKeys` `MapRemove` `Ptr` `HWnd` `VarGetType` |
| 解释器 / 控制 | `Call` `Eval` `Execute` `Assign` `FuncName` `SetError` `SetExtended` `Sleep` `Opt` `AutoItSetOption` `AdlibRegister` `AdlibUnRegister` `OnAutoItExitRegister` `OnAutoItExitUnRegister` `Binary` `BinaryLen` `BinaryMid` `BinaryToString` |
| 文件 / 目录 | `FileOpen` `FileClose` `FileFlush` `FileRead` `FileReadLine` `FileWrite` `FileWriteLine` `FileExists` `FileGetSize` `FileGetTime` `FileGetAttrib` `FileGetLongName` `FileGetShortName` `FileGetPos` `FileGetEncoding` `FileReadToArray` `FileFindFirstFile` `FileFindNextFile` `FileSetPos` `FileSetEnd` `FileSetTime` `FileChangeDir` `FileDelete` `FileCopy` `FileMove` `FileSetAttrib` `DirCreate` `DirRemove` `DirGetSize` `DirCopy` `DirMove` |
| INI | `IniRead` `IniWrite` `IniDelete` `IniReadSection` `IniReadSectionNames` `IniRenameSection` `IniWriteSection` |
| 进程 / 执行 / 标准 IO | `Run` `RunWait` `ProcessWait` `ProcessWaitClose` `ProcessGetStats` `ProcessSetPriority` `ProcessList` `ProcessExists` `ProcessClose` `StdoutRead` `StderrRead` `StdinWrite` `StdioClose` |
| 网络 | `InetGet` `InetGetInfo` `InetGetSize` `InetRead` `InetClose` `FtpSetProxy` `HttpSetProxy` `HttpSetUserAgent` `Ping` `TCP*` `UDP*` |
| 环境 / 计时 / 控制台 | `EnvGet` `EnvSet` `EnvUpdate` `TimerInit` `TimerDiff` `ConsoleWrite` `ConsoleWriteError` `ConsoleRead` |
| 注册表 / 剪贴板 / 驱动器（仿真） | `RegRead` `RegWrite` `RegDelete` `RegEnumKey` `RegEnumVal` `ClipGet` `ClipPut` `DriveGetDrive` `DriveGetType` `DriveGetFileSystem` `DriveGetLabel` `DriveGetSerial` `DriveSpaceTotal` `DriveSpaceFree` `DriveStatus` |
| 原生结构 / 调用（仿真） | `DllStructCreate` `DllStructGetData` `DllStructSetData` `DllStructGetSize` `DllStructGetPtr` `IsDllStruct` `DllCall` `DllOpen` `DllClose` |

---

## 七、里程碑建议

| 里程碑 | 范围 | 出口条件 |
| ---- | ---- | ---- |
| **M0 · 通用清零** | 列 A 的 67 项 | ✅ **已完成**：`cargo test` 全绿，新增 `docs/函数实现状态.tsv` |
| **M1 · 真实脚本去阻塞** | 列 B 的 P0（`FileGetVersion`/`MemGetStats`/`IsAdmin`/`FileRecycle`/`DllCallback*`/`ObjGet`/`IsObj`/`ShellExecute`/`Shutdown`） | `au3 evaluate` 在真实脚本上不再卡在这些调用；每个函数有单测 |
| **M2 · 无头 GUI 仿真** | 列 C 的 P0 子集→ 逐步覆盖 165 | `GUICreate`/`GUICtrl*`/`Win*` 可回答，`GUIGetMsg` 空转返回 `0`；真实脚本能走完整消息循环 |
| **M3 · 仿真完善** | 列 B 剩余 + 列 C 剩余 | 注册表/驱动器/回调与界面反馈全可答 |

---

## 八、验收标准

1. **语义可判定**：新增函数要么给出真实结果，要么按 AutoIt 语义失败并置 `@error`；
   不允许"静默编造值"（与 README「不静默编造值」一致）。
2. **受执行配置约束**：写文件/注册表/网络/启动进程等副作用必须走
   `ExecutionProfile`，`deterministic()` 下拒绝。
3. **归层正确**：平台无关与宿主进程/网络进 `builtins.rs`/`common/`
   （`proc.rs`/`net.rs`），Windows 语义进 `winemu/`；
   `Platform` trait 之外不新增平台耦合。
4. **有回归测试**：每个函数一个用例，覆盖正常路径、边界（空值/不存在/权限）与 `@error` 行为。
5. **可复现**：涉及随机/时间的函数在 `deterministic()` 下结果稳定。

---

## 附录：全量状态表

逐函数（403 行）的状态、归类、优先级见
[`docs/函数实现状态.tsv`](./函数实现状态.tsv)（列：`函数 / 状态 / 归类 / 归属层 / 优先级 / 说明`）。
